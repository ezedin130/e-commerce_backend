spring:
  application:
    name:payroll_system

  datasource:
    url: jdbc:mysql://localhost:3306/e_commerce
    username: root
    password: ''
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: false
        dialect: org.hibernate.dialect.MySQL8Dialect