spring:
  application:
    name:e_commerce

  datasource:
    url: jdbc:mysql://localhost:3306/e_commerce
    username: root
    password: 'root'
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: false
        dialect: org.hibernate.dialect.MySQL8Dialect
